[phases.setup]
nixPkgs = ['...']

[phases.install]
cmds = ['...']

[phases.build]
cmds = ['...']

[start]
# Laravel Octane avec FrankenPHP pour performances optimales
cmd = "php artisan octane:start --server=frankenphp --host=0.0.0.0 --port=$PORT"
