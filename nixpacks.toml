[phases.setup]
nixPkgs = ['...']

[phases.install]
cmds = ['...']

[phases.build]
cmds = ['...']

[start]
# Note: startCommand et preDeployCommand sont définis dans .railway.toml
# Ce fichier nixpacks.toml est gardé pour compatibilité mais .railway.toml a la priorité
cmd = "frankenphp php-server --listen :$PORT"
