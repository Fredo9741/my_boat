{
    admin off
    auto_https off
}

:{$PORT} {
    root * /app/public

    encode gzip zstd

    php_fastcgi 127.0.0.1:9000

    file_server

    header {
        -Server
        X-Content-Type-Options nosniff
        X-Frame-Options SAMEORIGIN
    }
}
